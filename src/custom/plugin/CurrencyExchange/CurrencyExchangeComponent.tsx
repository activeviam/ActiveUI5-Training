import React, { FC } from "react"
import { WidgetPluginProps } from "@activeviam/activeui-sdk";

export const CurrencyExchangeComponent: FC<WidgetPluginProps> = (props) => {
    return <div>This is a currency exchange component</div>
}